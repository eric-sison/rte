import { TextEditor } from '@rte/components/editor/TextEditor';

export default function Home() {
  return (
    <main className="h-screen w-screen flex items-center justify-center">
      <TextEditor />
    </main>
  );
}
